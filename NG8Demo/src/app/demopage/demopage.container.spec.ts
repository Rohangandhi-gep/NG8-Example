import { Demopage.Container } from './demopage.container';

describe('Demopage.Container', () => {
  it('should create an instance', () => {
    expect(new Demopage.Container()).toBeTruthy();
  });
});
