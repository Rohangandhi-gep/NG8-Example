import { DemopageContainer } from './demopage.container';

describe('Demopage.Container', () => {
  it('should create an instance', () => {
    expect(new DemopageContainer()).toBeTruthy();
  });
});
