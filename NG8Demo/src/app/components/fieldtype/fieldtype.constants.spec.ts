import { FieldtypeConstants } from './fieldtype.constants';

describe('Fieldtype.Constants', () => {
  it('should create an instance', () => {
    expect(new FieldtypeConstants()).toBeTruthy();
  });
});
