import { FieldtypeContainer } from './fieldtype.container';

describe('Fieldtype.Container', () => {
  it('should create an instance', () => {
    expect(new FieldtypeContainer()).toBeTruthy();
  });
});
